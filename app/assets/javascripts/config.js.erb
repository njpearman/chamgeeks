if(typeof(ChamGeeks) === 'undefined') {
  ChamGeeks = {};
}

ChamGeeks.config = {
  'stripeKey': '<%= Rails.application.config.stripe_key %>',
  'merchantGeekImage': '<%= asset_path("merchant-geek") %>'
};
